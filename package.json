{
  "name" : "bizarre-calendar",
  "version" : "1.0.0",
  "description" : "codecheck challenge",
  "scripts" : {
    "test": "codecheck",
    "build": "tsc --outDir app app/typescript/main.ts",
    "build-and-test": "npm run build && npm run test"
  },
  "author" : "Givery",
  "devDependencies" : {
    "chai" : "^3.5.0",
    "codecheck" : "^0.5.6"
  }
}